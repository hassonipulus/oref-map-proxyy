#!/bin/bash
# Deploy web/ to Cloudflare Pages (project: oref-map).
# Requires wrangler authentication: npx wrangler login
set -e
cd "$(dirname "$0")"
npx wrangler pages project create oref-map
npx wrangler pages deploy web/ --project-name oref-map 
